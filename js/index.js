window.addEventListener('load', function() {
    const imageText = document.querySelector('.txtBkgrnd');
    imageText.style.opacity = 1;
    imageText.style.transform = 'scale(1)';
  });


  window.addEventListener('load', function() {
    const logoAnim = document.querySelector('.logoAnim');
    logoAnim.style.opacity = 1;
    logoAnim.style.transform = ' TranslateX(0) scale(1)';
  });